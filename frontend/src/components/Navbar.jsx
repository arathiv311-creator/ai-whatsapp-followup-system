function Navbar({ title }) {
  return (
    <header className="navbar">
      <h1>{title}</h1>

      <div className="user-info">
        AI WhatsApp System
      </div>
    </header>
  );
}

export default Navbar;